{
  "name": "onboarding",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "set NODE_ENV=qa && nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "set NODE_ENV=qa && nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "nuxt": "^3.7.0"
  },
  "dependencies": {
    "test-package": "workspace:*"
  }
}
