{
  "name": "c1-micro-app-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm --stream -r run build",
    "dev": "pnpm --stream -r run dev",
    "preview": "pnpm --stream -r run preview"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20.15.0",
    "pnpm": ">=8"
  }
}
